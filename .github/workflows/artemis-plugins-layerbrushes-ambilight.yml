name: Artemis.Plugins.LayerBrushes.Ambilight - Build and upload

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - 'src/LayerBrushes/Artemis.Plugins.LayerBrushes.Ambilight/**'

jobs:
  upload:
    uses: ./.github/workflows/plugin-upload.yml
    with:
          projectPath: 'src/LayerBrushes/Artemis.Plugins.LayerBrushes.Ambilight/Artemis.Plugins.LayerBrushes.AmbilightV2.csproj'
    secrets:
      WORKSHOP_PAT: ${{ secrets.WORKSHOP_PAT }}

